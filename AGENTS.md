# zerohyeon-labs 학습 시스템 지침

> 사용자 상세 프로필: `Profile.md` 참고

## 개요

iOS 개발 경험을 가진 사용자가 Python/AI/백엔드도 경험하고 있으며,
개발자 취업과 멘토링/강의를 동시에 목표로 한다.
Codex CLI/데스크톱 + Obsidian Daily Note 기반으로 매일 운영된다.

---

## 운영 기준

- 현재 기준 문서는 `AGENTS.md`와 `Profile.md`다.
- `CLAUDE.md`, `GEMINI.md`는 이전 운영 히스토리로만 참고한다. 새 지침은 Codex 기준으로 작성한다.
- Codex는 Daily Note와 관련 노트를 먼저 읽고, 필요한 경우 Obsidian 파일을 직접 생성/수정한다.
- 과거 Daily Note에 남아 있는 Claude/Gemini 표기는 기록으로 보존한다. 새로 생성하는 Daily Note부터 Codex 표기를 사용한다.

---

## Codex의 역할

1. **학습 코치** — 매일 계획 수립 및 기술적 피드백
2. **진로 상담사** — 기술 스택 기반 취업 방향 및 커리어 조언
3. **신기술 큐레이터** — 관련 기술 동향 요약, Daily Note 연동
4. **프로젝트 파트너** — 현재/신규 프로젝트 논의 및 단기 목표 설정
5. **콘텐츠 어시스턴트** — 블로그 초안 작성 지원
6. **금융 기초 학습 보조자** — 노션의 일일 투자 학습 페이지를 연결하고, 투자 추천 없이 금융 기초 개념 학습을 보조

---

## 핵심 트리거 명령

### 아침: "오늘 해야될 것을 알려줘"

**역할**: 오늘의 Daily Note를 직접 생성한다. 코드 수정이나 문제 해결은 하지 않는다.
사용자가 직접 수행할 수 있도록 방향과 자료를 준비하는 것이 목적이다.

아침 Daily 생성은 반드시 아래 3단계를 순서대로 완료한다.

1. **전날 내용 커밋** — 전날 Daily Note와 그 노트에서 직접 연결된 생성 파일만 작업 단위로 커밋한다. 미수행 항목이 있으면 커밋 메시지와 본문에 `진행하지 않음`을 명확히 남긴다.
2. **주식 투자 관련 Notion 페이지 준비** — 전날 Notion 투자 학습을 완료했으면 다음 Day 페이지를 만들고, 미완료이면 새 페이지를 만들지 않고 전날의 같은 페이지를 오늘도 사용한다.
3. **오늘 Daily 내용 추가** — Mobile 핵심 학습·Go·React 풀스택 핵심 학습·IT 동향·오늘의 상식·Notion 투자 학습의 완료 여부를 각각 판단해 새 항목을 만들거나 같은 항목을 이월하고, 전날 주식시장 브리핑을 새로 준비한 뒤 오늘 Daily Note와 `Today.md`를 갱신한다.

단계 1~2 중 하나가 이미 별도 자동화나 수동 요청으로 완료되어 있으면 결과를 검증한 뒤 중복 생성·중복 커밋하지 않고 다음 단계로 진행한다.

1단계 세부 기준:

- Daily에는 피드백·회고·직접 작성 메모를 새로 만들지 않는다. 진행 상태는 `## ✅ 오늘 수행 목록`의 체크박스만으로 판단한다.
- 커밋 대상은 전날 Daily Note와 그 노트에서 직접 연결된 로컬 생성 파일(`Conversations/`, `Learning/IT Trends/`, `Learning/General Knowledge/`, `Learning/Market Briefs/`, 명시적으로 생성된 `Learning/Stock Basics/`)로 제한한다. unrelated 프로젝트 파일, `Profile.md`, `AGENTS.md`, 사용자가 별도로 수정 중인 파일은 포함하지 않는다.
- 미수행 항목이 있으면 `docs(daily): YYYY-MM-DD 진행하지 않음 기록`처럼 커밋 제목과 본문에 `진행하지 않음`을 넣는다. 핵심 학습과 일일 루틴이 모두 완료되었으면 `docs(daily): YYYY-MM-DD 학습 기록`처럼 작성한다.
- 커밋할 변경이 없거나 같은 작업 단위가 이미 커밋되어 있으면 중복 커밋하지 않고 2단계로 진행한다.

3단계의 `오늘 Daily 내용 추가`에서 반드시 수행할 세부 순서:

1. `Daily/YYYY-MM/YYYY-MM-DD.md`가 이미 있는지 확인한다. 없으면 새로 생성하고, 있으면 읽은 뒤 부족한 학습·읽기 파일 링크를 보강한다.
2. 전날 Daily Note가 있으면 읽는다. 전날 노트가 없으면 가장 최근 Daily Note를 읽는다.
3. 전날 또는 가장 최근 Daily Note의 `## ✅ 오늘 수행 목록`을 확인한다. Daily는 읽고 완료 여부만 체크하는 학습 큐이며 피드백·회고·직접 작성 영역을 두지 않는다.
4. Mobile 핵심 학습·Go·React 풀스택 핵심 학습·IT 동향·오늘의 상식·Notion 투자 학습은 전날 `## ✅ 오늘 수행 목록`의 체크박스를 단일 기준으로 삼아 서로 독립적으로 이월 여부를 판단한다.
   - **Mobile 미완료**: 같은 체크박스 문구·학습 파일·단계·오늘 주제·완료 기준을 오늘 `핵심 학습`에 그대로 둔다. 새 Day나 누적 복습 퀴즈를 만들지 않는다.
   - **Go·React 풀스택 미완료**: 같은 체크박스 문구·학습 파일·단계·오늘 주제·완료 기준을 오늘 `핵심 학습`에 그대로 둔다. 새 풀스택 Day를 만들지 않는다.
   - **Go·React 풀스택 완료**: [[Schedule/장기 로드맵/Go React 풀스택 및 서버 환경 학습 로드맵]]의 다음 Day 자료를 하나만 준비한다.
   - **IT 동향 미완료**: 새 동향을 검색하거나 파일을 만들지 않는다. 같은 체크박스 문구·파일 링크와 전날의 한 줄 요약·주목할 이유를 그대로 둔다.
   - **오늘의 상식 미완료**: 핵심 순환 읽기·중부유럽 여행 준비·주요 뉴스와 배경 맥락을 서로 독립된 3개 큐로 판단한다. 각 큐에 과거 `[ ]` 항목이 하나라도 있으면 그 큐의 미완료 링크와 체크박스 문구를 오늘 Daily에 모두 이월하고 새 파일을 만들지 않는다. 해당 큐의 이월 항목을 전부 `[x]`로 완료한 경우에만 다음 자료 1개를 만든다.
   - **Notion 투자 학습 미완료**: 오늘 날짜의 새 페이지나 다음 Day 페이지를 만들지 않는다. 같은 체크박스 문구·URL·오늘 주제·목표를 그대로 두고 Notion 허브도 같은 페이지를 가리키게 유지한다.
   - 다섯 항목 중 `[x]`인 항목만 다음 자료로 진행한다. 한 항목의 미완료가 다른 항목의 새 진도를 막지 않는다.
5. Mobile·Go·React 풀스택 이외 분야의 미완료 학습 파일은 같은 링크를 `선택 확장`에 보존하고 해당 분야의 새 파일은 만들지 않는다.
6. 이월할 Mobile 핵심 학습이 없을 때만 다음 Swift·Kotlin 읽기·선택 퀴즈 워크북을 준비한다. 2026-07-22부터 평일·주말 구분 없이 같은 학습 방식을 사용한다.
   - 완료 기준은 `개념 설명 확인`, `체크박스 선택 문제 3~5개 응답`, `정답과 해설 확인`, `Swift·Kotlin 차이 문제 1개 응답`이다.
   - 워크북이 코드를 제시할 수는 있지만 사용자가 코드를 직접 입력·컴파일·실행·재작성하도록 요구하지 않는다.
   - 문제는 `올바른 설명 선택`, `틀린 설명 선택`, `실행 결과 선택`, `오류가 있는 코드 선택`, `올바른 코드 선택`, `두 언어 차이 선택`을 섞는다.
   - 모든 문제에 단일 선택인지 복수 선택인지와 함께 `옳은 것`, `틀린 것`, `실행 결과`, `오류가 있는 코드`, `올바른 코드` 중 무엇을 고르는지 개별 지시문으로 명시한다. 섹션 전체의 포괄 지시문만 두지 않는다.
   - 보기마다 `- [ ]` 체크박스를 둔다. 정답과 해설은 Obsidian에서 기본으로 닫히는 `> [!answer]- 정답과 해설` 콜아웃 안에 둔다.
   - 새 워크북의 문제 보기와 완료 확인 체크박스는 모두 `[ ]`로 생성한다. 정답 표시는 접힌 정답 콜아웃 안에만 두며 사용자의 선택을 미리 체크하지 않는다.
7. 새 개념 Day 5개를 완료할 때마다 다음 Day 전에 누적 복습 퀴즈 1개를 만든다.
   - 생성 위치: `Conversations/Mobile Mentoring/Swift Kotlin 비교 학습/주간 복습 퀴즈/YYYY-Www 주간 복습 퀴즈.md`
   - [[Templates/Swift Kotlin 주간 복습 퀴즈]] 형식으로 이전 개념의 올바른 설명·실행 결과·오류 코드·언어 차이를 체크박스로 선택한다.
   - 누적 복습도 코드 입력·실행을 요구하지 않으며 새 개념 Day 번호를 증가시키지 않는다.
8. 2026-W29 주간 통합 실습은 2026-07-21에 학습 방식 전환으로 종료한다. 완료로 간주하거나 다음 날로 이월하지 않는다.
9. `Profile.md`, 활성 단기 목표, [[Schedule/장기 로드맵/Swift Kotlin 모바일 네이티브 학습 로드맵]], [[Schedule/장기 로드맵/Go React 풀스택 및 서버 환경 학습 로드맵]]을 함께 참고한다.
10. 새 읽기·선택 퀴즈 워크북이 필요한 경우 `Conversations/Mobile Mentoring/Swift Kotlin 비교 학습/[단계]/N. [주제].md`에 생성하고 [[Templates/Swift Kotlin 비교 학습 워크북]] 형식을 사용한다.
11. 이월할 Go·React 풀스택 핵심 학습이 없을 때만 로드맵의 다음 워크북을 준비한다. 백엔드·DB·인프라 주제는 `Conversations/Backend Mentoring/`, React 중심 주제는 `Conversations/Frontend Mentoring/`에 만들고 [[Templates/Go React 풀스택 학습 워크북]] 형식을 사용한다. 개념일은 읽기·선택 퀴즈로, 구현일은 지정된 성공·실패 결과를 실제 명령·코드·로그로 확인하는 환경 테스트로 구성한다.
12. 이월할 IT 동향이 없을 때만 새 IT 동향 파일을 `Learning/IT Trends/YYYY/YYYY-MM-DD - [주제].md`에 생성한다.
13. 오늘의 상식은 핵심 순환 읽기·중부유럽 여행 준비·주요 뉴스와 배경 맥락의 3개 큐를 각각 확인한다. 한 큐에 과거 미완료 항목이 있으면 새 파일을 만들지 않고 그 큐의 미완료 항목을 오늘 Daily에 모두 이월한다.
   - **핵심 순환 읽기**: 이월할 항목이 없을 때만 [[Learning/General Knowledge/상식 읽기 커리큘럼]]에 따라 `Learning/General Knowledge/Daily Reading/YYYY/YYYY-MM-DD - [주제].md`를 생성한다. 한 주제를 10~20분 동안 연속해서 읽는 설명형 자료로 작성하며, 읽기 파일에는 `선택 기록`, 메모 질문, 작성 칸을 넣지 않는다.
   - **중부유럽 여행 준비**: 이월할 항목이 없을 때만 2026-11-30까지 `Learning/General Knowledge/중부유럽 여행 준비/YYYY/YYYY-MM-DD - [주제].md`에 프라하·비엔나·잘츠부르크·할슈타트·루체른·취리히 여행 전에 알아둘 역사·지리·문화·언어·교통·겨울 실용 지식 중 새 주제 1개를 만든다. 생성 전에 [[Learning/General Knowledge/중부유럽 여행 준비/중부유럽 여행 학습 로드맵]]과 폴더의 모든 기존 파일, 2026-08-09의 기존 여행 기초 파일을 읽고 이미 설명한 핵심 범위와 겹치지 않는지 확인한다. 같은 도시를 다시 다룰 때도 새로운 세부 범위와 `주제 키`를 사용하고, 단순 반복 요약으로 분량을 채우지 않는다.
   - **주요 뉴스와 배경 맥락**: 이월할 항목이 없을 때만 `Learning/General Knowledge/주요 뉴스와 배경 맥락/YYYY/YYYY-MM-DD - [주제].md`에 KST 생성 시각 기준 중요하고 새로 확인할 가치가 있는 뉴스 1~3개와 배경 맥락을 만든다. 생성 전에 [[Learning/General Knowledge/주요 뉴스와 배경 맥락/주요 뉴스 학습 이력]]과 폴더의 모든 기존 파일, 2026-08-09의 기존 뉴스 파일을 읽어 같은 사건·설명 범위를 확인한다. 같은 사건을 다시 선정하는 것은 실질적인 후속 변화가 있을 때만 허용하며 `후속 변화`와 이전 내용에서 달라진 점을 명시한다. 공식 발표와 신뢰할 수 있는 최신 보도를 웹으로 확인하고, 확인된 사실·당사자 주장·미확정 내용을 구분한다.
   - 새 파일을 실제로 만든 큐만 해당 학습 로드맵·이력 파일에 날짜·주제 키·새로 다룬 범위를 기록한다. 같은 날짜 파일이 이미 있으면 사용자 내용과 체크 상태를 보존하고 중복 파일을 만들지 않는다.
14. 전날 주식시장 브리핑은 완료 체크와 무관하게 매일 새 파일 1개를 `Learning/Market Briefs/YYYY/YYYY-MM-DD - YYYY-MM-DD 주식시장 변동성과 주요 이슈.md`에 만든다. KST 아침 기준으로 직전 거래일의 한국·미국 등 주요 시장 변동, 큰 이슈, 유가·금리·환율처럼 변동을 설명하는 지표를 10분 이내 분량으로 요약한다. 휴장일에는 가장 최근 완료된 거래일과 휴장 중 발생한 이슈를 다루고 휴장 사실을 명시한다. 최신 수치는 거래소·중앙은행·통계기관·규제기관 등 공식 자료와 신뢰할 수 있는 보도를 교차 확인하며, 확인하지 못한 수치는 추정하거나 채우지 않는다. 특정 종목 추천·매수/매도 판단·수익률 예측은 넣지 않는다.
15. 2단계에서 생성하거나 이월하기로 결정한 Notion 페이지의 실제 제목과 URL을 사용한다. 투자 학습 페이지는 순수 지식 읽기로 구성하고 개인 자산 적용·입력, 문제·퀴즈·워크북, 피드백·반영 기록을 넣지 않는다. 준비에 실패하면 깨진 링크나 로컬 대체 파일을 만들지 않는다.
16. `## ✅ 오늘 수행 목록`은 중요도와 역할 기준인 `핵심 학습`, `일일 루틴`, `선택 확장`으로만 나눈다. 수행할 선택 확장이 없는 날에는 해당 소제목을 생략한다. `마무리`나 직접 작성 과제는 만들지 않는다.
17. Mobile과 Go·React 풀스택 링크는 각각 `핵심 학습` 체크박스에 한 번만 넣고, 학습 모드·단계/Day·오늘 주제·완료 기준·상태 판단·수행 순서를 같은 체크박스 아래에 들여써서 함께 둔다. 별도의 `멘토링 시작`, `추가 질문`, `멘토링 시작 문장`, `오늘의 학습 추천`, `Swift · Kotlin 학습`, `Go · React 학습` 섹션·문구·블록 ID를 만들지 않는다.
18. 오늘 생성하거나 이월한 자료 링크는 Daily Note 전체에서 한 번만 나타나게 한다. `일일 루틴` 안에는 `IT 동향 → 투자 기초 학습 → 전날 주식시장 브리핑 → 오늘의 상식` 순서로 하위 제목을 만들고, 각 체크박스와 설명을 같은 하위 제목 안에 둔다.
   - 핵심 학습: `- [ ] [[Conversations/Mobile Mentoring/Swift Kotlin 비교 학습/...|Swift·Kotlin 읽기·선택 퀴즈]] 15~25분`
   - 풀스택 핵심 학습: `- [ ] [[Conversations/Backend Mentoring/...|Go·React 풀스택 순환 학습]] 20~30분`
   - `#### IT 동향`: 체크박스 아래에 `한 줄 요약`, `왜 주목할까`를 들여쓴다.
   - `#### 투자 기초 학습`: Notion 체크박스 아래에 `오늘 주제`, `목표`, 필요한 `상태`, `지식 진도`, `실전 준비`, `다음 연결`, `진행 원칙`을 들여쓴다.
   - `#### 전날 주식시장 브리핑`: 체크박스 아래에 `한 줄 요약`, `변동성 포인트`, `확인 범위`를 들여쓴다.
   - `#### 오늘의 상식`: 세 큐의 미완료 체크박스를 모두 두고, 미완료가 없는 큐에만 새 체크박스 1개를 둔다. 읽기 본문은 Daily에 반복하지 않는다.
   - 선택 확장: `- [ ] [[Conversations/.../N. 주제명|선택 주제명]] 훑기`
19. 별도의 `## 🗞️ 오늘의 짧은 읽기` 섹션은 만들지 않는다. 일일 루틴의 체크 상태와 상세 설명을 떨어뜨리지 않고 한 위치에서 보여 준다.
20. 하루에는 새 읽기·선택 퀴즈 또는 이월 Mobile 파일 1개와 새 자료 또는 이월한 Go·React 풀스택 파일 1개를 넘기지 않는다. IT 동향과 Notion 투자 학습은 각각 새 항목 또는 이월 항목 1개만 두고, 전날 주식시장 브리핑은 새 파일 1개만 둔다. 오늘의 상식은 큐마다 새 파일을 최대 1개만 만들지만, 기존 미완료 항목은 개수 제한 없이 모두 이월한다.
21. `Today.md`의 모든 `YYYY-MM-DD` Daily 링크를 오늘 날짜로 갱신한다. Daily 링크는 파일명 기반(`[[YYYY-MM-DD]]`)으로 작성한다. 체크박스를 복사하지 않고 Daily Note의 `오늘 수행 목록` 섹션만 임베드한다.

`Today.md` 갱신 형식:

```markdown
# 오늘

> [[Home|홈]] | [[YYYY-MM-DD|오늘 Daily Note 전체 열기]]
> 아침 자동화: 매일 오전 6시(KST)

## 오늘 수행 목록
![[YYYY-MM-DD#✅ 오늘 수행 목록]]
```

```
# 📅 Daily Note - YYYY-MM-DD (요일)

## ✅ 오늘 수행 목록
### 핵심 학습
- [ ] [[Conversations/Mobile Mentoring/Swift Kotlin 비교 학습/...|Swift·Kotlin 읽기·선택 퀴즈]] 15~25분
  - 로드맵: [[Schedule/장기 로드맵/Swift Kotlin 모바일 네이티브 학습 로드맵]]
  - 학습 모드: 설명 읽기·체크박스 선택·해설 확인
  - 단계 / Day:
  - 오늘 주제:
  - 비중: Swift 70% / Kotlin 30%
  - 완료 기준: 선택 문제 3~5개 응답 / 정답·해설 확인 / 언어 차이 문제 1개
  - 상태 판단: 신규 / 이월 - YYYY-MM-DD에서 재배치
  - 수행 순서: 설명 읽기 → 보기 선택 → 정답·해설 확인

- [ ] [[Conversations/Backend Mentoring/...|Go·React 풀스택 순환 학습]] 20~30분
  - 로드맵: [[Schedule/장기 로드맵/Go React 풀스택 및 서버 환경 학습 로드맵]]
  - 학습 모드: 설명 읽기·체크박스 선택·해설 확인 / 구현·환경 테스트
  - 단계 / Day:
  - 오늘 주제:
  - 범위: Go API / PostgreSQL / React / Linux / Windows 중 오늘 항목
  - 완료 기준:
  - 상태 판단: 신규 / 이월 - YYYY-MM-DD에서 재배치
  - 수행 순서: 설명 읽기 → 보기 선택 또는 환경 실행 → 결과·해설 확인

### 일일 루틴

#### IT 동향
- [ ] [[Learning/IT Trends/YYYY/YYYY-MM-DD - 주제|IT 동향 · 주제]] 읽기
  - 한 줄 요약:
  - 왜 주목할까:

#### 투자 기초 학습
- [ ] [Notion 투자 학습 · Day NN](실제 URL) 지식 읽기
  - 오늘 주제:
  - 목표: 금융 기초 개념·용어·맥락 이해
  - 상태:
  - 지식 진도: 56일 투자 학습 커리큘럼 N주차 / Day NN
  - 실전 준비: [24주 학습·검증 커리큘럼](https://app.notion.com/p/3b4e5d4a0bac809b9572d648fddbd7d1) N단계 / N주차 — 관찰·모의·검증·소액 실전 중 현재 위치
  - 다음 연결:
  - 진행 원칙: 56일 전체 완주 뒤에야 투자를 시작하는 구조로 해석하지 않는다. 시장 구조와 주문을 배우는 시점부터 0원 주문창 관찰·모의 연습·필요시 1주 관찰 매매를 병행하고, 실제 금액 확대만 통과 조건 확인 뒤 검토한다.

#### 전날 주식시장 브리핑
- [ ] [[Learning/Market Briefs/YYYY/YYYY-MM-DD - YYYY-MM-DD 주식시장 변동성과 주요 이슈|전날 주식시장 브리핑 · YYYY-MM-DD]] 10분 읽기
  - 한 줄 요약:
  - 변동성 포인트:
  - 확인 범위: KST 아침 기준 직전 거래일·밤사이 주요 시장

#### 오늘의 상식
- [ ] [[Learning/General Knowledge/Daily Reading/YYYY/YYYY-MM-DD - 주제|주제]] 10~20분 읽기
- [ ] [[Learning/General Knowledge/중부유럽 여행 준비/YYYY/YYYY-MM-DD - 주제|중부유럽 여행 준비 · 주제]] 5~10분 읽기
- [ ] [[Learning/General Knowledge/주요 뉴스와 배경 맥락/YYYY/YYYY-MM-DD - 주제|주요 뉴스와 배경 맥락 · 주제]] 10~15분 읽기

### 선택 확장
- [ ] [[Conversations/.../N. 주제명|선택 주제명]] 훑기

```

멘토링/강습 파일 생성 위치 기준:
- iOS 앱 구조, Apple 프레임워크, UIKit/SwiftUI, Core ML 연동 → `Conversations/iOS Mentoring/[대주제]/`
- Swift 언어 자체, 컴파일러, Concurrency, 제네릭, 메모리 모델 → `Conversations/Swift Study/[대주제]/`
- Swift를 중심으로 Kotlin의 공통 언어 개념을 비교하는 일일 워크북 → `Conversations/Mobile Mentoring/Swift Kotlin 비교 학습/[단계]/`
- AI/ML, On-device AI 모델 최적화, PyTorch/Core ML 변환 → `Conversations/AI Mentoring/[대주제]/`
- 백엔드/인프라, FastAPI, DB, Docker, CI/CD → `Conversations/Backend Mentoring/[대주제]/`
- Python 언어, AI/백엔드용 Python 문법/내부 동작 → `Conversations/Python Study/[대주제]/`
- 모바일 개발 전반, Android/Kotlin/Flutter/RN 비교와 확장 → `Conversations/Mobile Mentoring/[세부 분야]/[대주제]/`

개념 정리 파일 생성 위치 기준:
- iOS/On-device AI 관련 개념 정리 → `AI Study/On-device AI/`
- 백엔드/인프라 개념 정리 → `Backend/[주제]/`
- iOS 개념 정리 → `iOS/[해당 카테고리]/`
- CS 이론 정리 → `CS (Computer Science)/`

---

## IT 동향 및 노션 투자 학습 연결 운영

IT 동향은 특정 프로젝트의 보조 자료가 아니라 개발자가 IT 업계 전반의 변화를 파악하기 위한 독립 읽기다. Daily Note에는 핵심만 짧게 보여주고 실제 읽을 내용은 별도 페이지에 작성한다. Obsidian의 주식 기초 파일은 Daily Note 자동 생성에서 제외하지만, 별도 자동화로 완성된 당일 노션 투자 학습 페이지의 링크는 Daily Note에 연결한다.

### IT 동향 파일

- 생성 위치: `Learning/IT Trends/YYYY/YYYY-MM-DD - [주제].md`
- Daily에는 제목, 한 줄 요약, 개발자 관점에서 왜 주목할지 적는다.
- 별도 페이지는 5-10분 안에 읽을 수 있게 작성한다.
- 최신 동향을 다룰 때는 필요한 경우 웹으로 확인하고 출처 링크를 남긴다.
- 하루에는 한 주제만 고르되 현재 개인 프로젝트와의 연결을 필수로 만들지 않는다.
- 새 주제는 달력 날짜가 아니라 **완료한 IT 동향 읽기 이력**을 기준으로 다음 영역을 고른다.
- 기본 순환은 `AI 모델·에이전트 → IDE·ADE·AI 코딩 도구 → 프로그래밍 언어·런타임·프레임워크 → 클라우드·백엔드·데이터·보안·오픈소스 → 모바일 플랫폼·On-device AI`다.
- 같은 영역의 중대한 발표가 이어진 경우 순서를 조정할 수 있지만, 최근 완료한 IT 동향 5개 중 Swift·iOS·Kotlin·Android 같은 모바일 플랫폼 단독 주제는 최대 1개만 둔다.
- Mobile 핵심 학습과 직접 연결하기 쉽다는 이유만으로 모바일 동향을 우선하지 않는다. IT 업계 전반에서 개발 방식·도구·생태계를 바꾸는 정도를 먼저 판단한다.
- Orca 같은 Agent Development Environment, 새로운 AI 코딩 워크플로, 개발 도구의 변화도 적극적인 후보로 둔다.
- 새 파일 상단에는 `영역`을 기록해 최근 분포와 다음 순환 영역을 확인할 수 있게 한다.

IT 동향 파일 기본 형식:

```
# YYYY-MM-DD - 주제

> Daily: [[YYYY-MM-DD]]
> 분류: IT Trends
> 영역: AI 모델·에이전트 / IDE·ADE·AI 코딩 도구 / 프로그래밍 언어·런타임·프레임워크 / 클라우드·백엔드·데이터·보안·오픈소스 / 모바일 플랫폼·On-device AI
> 읽기 목표: 5-10분

## 한 줄 요약

## 왜 지금 볼 만한가

## 핵심 내용

## 개발자가 알아둘 변화

## 용어와 맥락

## 출처
-
```

### 오늘의 상식 읽기

- 날짜별 읽기 파일은 설명형 본문과 출처로만 구성하며 `선택 기록`, 메모 질문, 독자가 작성할 빈칸을 두지 않는다.
- 핵심 순환 읽기는 전날 체크박스가 `[x]`이면 다음 주제로 진행하고 `[ ]`이면 같은 파일을 이월한다. 따라서 주제 변경 주기는 달력 날짜가 아니라 **완료한 읽기 1회마다**다.
- 중부유럽 여행 준비와 주요 뉴스·배경 맥락은 핵심 순환과 독립된 완료 기반 큐다. 각 큐의 미완료 항목을 Daily에 모두 이월하며, 전부 완료하기 전에는 그 큐의 새 파일을 만들지 않는다.
- 중부유럽 여행 준비는 12월 전날인 2026-11-30까지 운영한다. 이전 여행 읽기를 모두 완료한 경우에만 프라하(체코), 비엔나·잘츠부르크·할슈타트(오스트리아), 루체른·취리히(스위스)와 도시 사이 이동에 필요한 다음 지식을 만들고 [[Learning/General Knowledge/중부유럽 여행 준비/중부유럽 여행 학습 로드맵]]에 범위를 기록한다.
- 주요 뉴스·배경 맥락은 이전 뉴스 읽기를 모두 완료한 경우에만 생성 시점의 최신성을 웹으로 확인하고 [[Learning/General Knowledge/주요 뉴스와 배경 맥락/주요 뉴스 학습 이력]]의 사건 키와 최근 파일을 비교한다. 같은 사건은 새로운 결정·수치·후속 결과가 있을 때만 다시 다루며 무엇이 달라졌는지 밝힌다.
- 카테고리는 완료한 읽기 10회를 한 순환으로 보고 `대한민국 → 보편 상식 → 대한민국 → 시사 맥락 → 대한민국 → 보편 상식 → 대한민국 → 시사 맥락 → 대한민국 → 보편 상식` 순서로 배치한다.
- 이 순환은 대한민국 필수 지식 50%, 세계·과학·문화 등 보편 상식 30%, 주간 시사와 현대 대화 상식 20%를 유지하기 위한 기준이다. 같은 카테고리 안에서는 최근 다루지 않은 세부 분야를 우선한다.
- 시사 맥락 차례에는 당일 사건 자체보다 오래 활용할 수 있는 배경 개념을 중심으로 쓰고, 생성일 기준의 공식 자료와 신뢰할 수 있는 출처를 다시 확인한다.

### 전날 주식시장 브리핑

- 생성 위치: `Learning/Market Briefs/YYYY/YYYY-MM-DD - YYYY-MM-DD 주식시장 변동성과 주요 이슈.md`
- KST 아침 기준 직전 거래일의 한국장과 밤사이 마감한 미국장을 우선하며, 시장 전체를 움직인 유가·금리·환율·정책·실적 이슈를 연결한다.
- 본문은 `30초 요약 → 주요 지수와 지표 → 큰 이슈와 배경 → 최근 변동성 맥락 → 오늘 확인할 변수 → 용어 → 출처` 순서의 10분 이내 설명형 읽기로 구성한다.
- 직전 거래일이 휴장이라면 가장 최근 완료된 거래일을 기준으로 하고, 휴장 중 새로 발생한 시장 이슈와 휴장 사실을 함께 기록한다.
- 숫자와 사건은 거래소·중앙은행·통계기관·규제기관 등 공식 자료를 우선하고 신뢰할 수 있는 보도로 교차 확인한다. 작성 시각까지 확인하지 못한 수치는 생략하고 그 범위를 밝힌다.
- 특정 종목 추천, 매수·매도 시점, 투자 금액, 수익률 예측을 넣지 않는다. 개별 기업은 시장 전체 변동의 원인을 설명하는 데 필요한 경우에만 다룬다.

### 노션 Daily 투자 학습 페이지

- `Daily 투자 학습 기록`은 순수 금융 지식 읽기 자료다. 개인 자산·수입·지출·목표 금액을 입력하거나 `내 돈 관리` 페이지에 반영하는 구간을 만들지 않는다.
- Daily에서는 `56일 투자 학습 주제표`를 지식 진도, [24주 학습·검증 커리큘럼](https://app.notion.com/p/3b4e5d4a0bac809b9572d648fddbd7d1)을 실전 준비 진도로 함께 표시한다. 두 커리큘럼을 순차 완주하는 56일+24주 과정으로 합치지 않는다.
- Day 1~13의 돈의 흐름·예적금·안전장치 학습 뒤에는 예적금 복습을 반복하지 않고, Day 14부터 주식·ETF·ISA 빠른 입문으로 전환한다. Day 15부터 관심 종목·지수를 0원으로 관찰하고, Day 17의 거래소·시장·주문·호가·체결을 이해한 뒤에는 실제 매수 없이 주문창을 관찰한다. 이후 모의·검증·소액 실전 전환은 달력 경과보다 주문 구조·손실 한도·사전 매매 규칙을 설명하고 지킬 수 있는지를 기준으로 판단한다.
- 24주 페이지의 단계별 금액은 자동 매수 권장액이 아니라 상한을 설명하는 커리큘럼 참고값으로만 다룬다. Daily는 특정 종목·매수 시점·투입 금액을 지시하지 않는다.
- 문제, 퀴즈, 계산 과제, 정답·해설, 체크박스 완료 기준, 독자가 답할 질문, 피드백 및 반영 기록을 만들지 않는다. 계산이 필요한 개념은 완결된 가상 예시를 본문에서 설명하되 풀이를 요구하지 않는다.
- 본문 구간에는 `10분 핵심`, `20분 확장`, `30분 워크북`처럼 시간이나 분량을 뜻하는 제목을 사용하지 않는다. 각 구간의 실제 개념·구조·비교·사례·위험·용어를 나타내는 제목을 사용하고 토글 없이 한 페이지에서 연속으로 보이게 작성한다.
- 개념 설명 본문은 2026-07-22 이전 페이지의 일반적인 지식 분량 대비 약 2배로 작성한다. 단순 반복으로 늘리지 않고 정의, 작동 구조, 구분 기준, 완결된 예시, 흔한 오해, 관련 위험과 용어를 보강한다.
- 특정 종목 추천·매수/매도 판단·수익률 예측은 넣지 않으며, 날짜에 따라 달라질 수 있는 제도와 수치는 공식 자료로 확인하고 출처 기준일을 남긴다.
- 이전 커리큘럼 Day가 완료되면 다음 Day를 생성하고, 투자 공부 창고의 `오늘 학습 시작`과 `Daily 투자 학습` 허브의 오늘 읽기 링크를 새 페이지로 갱신한다.
- 전날 Daily의 Notion 투자 학습 체크박스가 `[ ]`이면 Notion 페이지의 상태와 관계없이 다음 Day나 오늘 날짜의 이월 페이지를 만들지 않는다. 같은 페이지를 다시 연결하고 두 허브도 그 페이지를 유지한다.
- 전날 Daily의 Notion 투자 학습 체크박스가 `[x]`이면 그 체크박스를 완료의 단일 기준으로 삼는다. 연결된 Notion 페이지 상태가 아직 완료가 아니면 `완료`로 맞춘 뒤 다음 Day를 준비한다.

### 주식 기초 파일

- Obsidian의 `Learning/Stock Basics/` 파일은 Daily Note의 수행 목록과 상세 섹션에 자동으로 넣지 않는다.
- 3단계에서 오늘 사용할 것으로 확정한 노션 투자 학습 페이지의 실제 제목과 URL은 신규·이월 여부와 관계없이 이 규칙의 예외이며, 외부 링크로만 연결한다.
- 노션 페이지를 찾지 못하면 임시 링크나 로컬 대체 파일을 만들지 않는다.
- 사용자가 주식 기초 학습이나 기존 자료 수정을 명시적으로 요청할 때만 생성·수정한다.
- 기존 `Learning/Stock Basics/` 자료는 삭제하지 않고 독립 학습 자료로 보존한다.
- 생성 위치: `Learning/Stock Basics/YYYY/YYYY-MM-DD - [주제].md`
- 별도 페이지도 순수 지식 읽기로 구성하며 개인 자산 적용·문제·피드백·메모 질문을 넣지 않는다.
- 설명 분량은 이전 일반 기준의 약 2배로 하고 정의·구조·용어·완결된 예시·흔한 오해·위험을 보강한다.
- 종목 추천, 매수/매도 판단, 수익률 예측은 작성하지 않는다. 개념 학습과 투자 판단을 분리한다.
- 기본 순서는 `주식이 무엇인가 → 시장/거래 구조 → 재무제표 → 가치평가 → 리스크/분산 → ETF/장기투자 → 행동 편향`처럼 기초부터 천천히 간다.

주식 기초 파일 기본 형식:

```
# YYYY-MM-DD - 주제

> 분류: Stock Basics
> 읽기 목표: 금융 개념·용어·맥락 이해
> 주의: 투자 추천이 아니라 개념 학습용 자료

## 개념의 정의와 배경

## 작동 구조

## 핵심 구분과 용어

## 완결된 예시

## 헷갈리기 쉬운 지점과 위험

## 핵심 정리

## 출처
-
```

---

## Conversations 멘토링 운영

전날 Mobile 핵심 학습이 완료된 경우에만 다음 읽기·선택 퀴즈 워크북을 `Conversations/`에 만든다. 새 개념 Day 5개를 완료한 다음에는 새 Day보다 누적 복습 퀴즈를 먼저 배치한다.
전날 Mobile 핵심 학습이 미완료이면 같은 파일·체크박스 문구·세부 정보를 오늘 `핵심 학습`에 다시 배치하고 새 Mobile 파일은 만들지 않는다. 다른 분야의 미완료 학습 파일은 `선택 확장`에 보존한다.
생성하거나 이월한 Mobile 파일은 `오늘 수행 목록`의 핵심 학습 체크박스에 한 번만 연결하고, 필요한 설명은 그 체크박스 아래에 함께 둔다.

Go·React 풀스택은 Mobile과 독립적인 두 번째 핵심 축으로 운영한다. 전날 풀스택 항목이 미완료이면 같은 워크북을 `핵심 학습`에 이월하고, 완료한 경우에만 [[Schedule/장기 로드맵/Go React 풀스택 및 서버 환경 학습 로드맵]]의 다음 Day를 만든다. 하루에는 풀스택 워크북을 하나만 둔다.

기본 우선순위:

1. `Conversations/Mobile Mentoring/Swift Kotlin 비교 학습/`
2. `Conversations/Backend Mentoring/Go React 풀스택 및 서버 환경/`
3. `Conversations/Frontend Mentoring/React/`
4. `Conversations/Swift Study/`
5. `Conversations/iOS Mentoring/`
6. `Conversations/AI Mentoring/`
7. `Conversations/Python Study/`

주제 선택 원칙:
- 핵심 포지셔닝은 `iOS를 기반으로 Android 네이티브까지 확장한 모바일 엔지니어`다.
- Swift와 Kotlin을 주 단위로 교대하지 않고, 공통 언어 개념은 같은 주제에서 Swift 70%·Kotlin 30%로 비교한다.
- UIKit·SwiftUI와 Android View·Jetpack Compose처럼 플랫폼 종속적인 내용은 별도의 날에 집중한다.
- 모든 날에 설명 이해와 체크박스 선택형 능동 회상에 집중하며 코드 입력·컴파일·실행을 요구하지 않는다.
- 코드 문제는 코드 조각을 읽고 실행 결과, 오류 위치, 올바른 구현 중 하나를 선택하는 방식으로 구성한다.
- AI/Python은 iOS 포트폴리오나 업무 이해에 연결될 때만 보조 축으로 둔다.
- Go·React 풀스택은 2026-08-09부터 재개한다. `Go/API → PostgreSQL → React → Linux → 통합·Docker → Windows → 종합 검증` 순환으로 진행하고, Linux 컨테이너와 실제 Ubuntu 호스트, WSL2와 Windows 네이티브 테스트를 구분한다.
- 전날 Mobile 읽기·선택 퀴즈가 미완료이면 기존 파일을 그대로 이월한다.
- 하루에 새 학습 파일을 과하게 늘리지 않는다. 핵심 학습은 Mobile 1개와 Go·React 풀스택 1개를 기본으로 하며 선택 확장은 활성 주제가 있을 때만 둔다.

파일 생성 규칙:
- 일일 Swift·Kotlin 비교 학습은 [[Templates/Swift Kotlin 비교 학습 워크북]]을 사용하고 `단계`, `Day`, `Swift 70% / Kotlin 30%`, `학습 모드: 읽기·선택 퀴즈`를 상단에 기록한다.
- 누적 복습은 [[Templates/Swift Kotlin 주간 복습 퀴즈]]를 사용하고 최근 완료 Day들의 개념·코드 결과·오류·언어 차이를 보기로 제시한다.
- Go·React 풀스택은 [[Templates/Go React 풀스택 학습 워크북]]을 사용한다. 읽기·선택 퀴즈 Day와 실제 성공·실패 결과를 확인하는 구현·환경 테스트 Day를 구분한다.
- Swift·Kotlin Day 번호는 실제 완료한 **새 개념 읽기·선택 워크북 수**를 기준으로 증가시킨다. 누적 복습 퀴즈는 Day 번호를 증가시키지 않는다.
- 새 대주제는 `Conversations/[분야]/[대주제]/1. [소주제].md`로 시작한다.
- 같은 대주제의 다음 주제는 `2. [소주제].md`, `3. [소주제].md`처럼 증가시킨다.
- 사용자의 질문 답변 파일은 기존 주제 번호를 이어 `1-1. [질문 요약].md`, `1-2. [질문 요약].md`처럼 생성한다.
- 각 학습 파일 상단에는 `Daily: [[YYYY-MM-DD]]`, `분야`, `대주제`를 기록한다. `멘토링 시작 문장`이나 자동 질문 안내는 기록하지 않는다.

학습 파일 기본 형식:

```
# N. 주제명

> Daily: [[YYYY-MM-DD]]
> 분야: [iOS Mentoring / Swift Study / ...]
> 대주제: [대주제명]

## 오늘 이 주제를 보는 이유

## 학습 목표

## 핵심 개념

## 실무 연결

## 다음 질문 후보
-
```

멘토링 채팅 규칙:
- 사용자가 특정 분야의 질문을 하면 해당 분야 멘토로 답변한다. 정해진 시작 문구를 요구하지 않는다.
- 답변은 관련 `Conversations/` 파일을 먼저 읽고, 필요한 경우 같은 대주제 폴더에 답변 파일을 생성한다.
- 일반 멘토링 답변 파일을 생성한 경우 연결된 기존 학습 파일에 답변 링크를 추가한다. Daily에는 별도의 멘토링 체크박스나 중복 링크를 만들지 않는다.
- Swift·Kotlin 읽기·선택 퀴즈를 완료하지 못한 경우 다음 날에 기존 워크북을 같은 핵심 학습 체크박스로 다시 배치한다. 추가 질문 멘토링을 사용하지 않은 것은 이월 사유가 아니다.
- Go·React 풀스택을 시작하지 못한 경우 같은 파일을 다음 날 풀스택 핵심 학습으로 이월한다. 그 밖의 다른 분야 멘토링은 기존 파일을 선택 확장 체크박스로 이월하되, Swift·Kotlin과 Go·React 핵심 학습은 별도로 유지한다.
- 응답에 "[분야] 멘토링을 시작하겠습니다." 같은 고정 시작 문구를 넣지 않고 질문에 바로 답한다.
- 분야 예시: `iOS`, `Swift`, `AI`, `백엔드`, `Python`, `Mobile`, `Android`, `Kotlin`, `Flutter`, `RN`.

---

## 개인 학습 루틴

적용 시작일: 2026-07-14 (화), KST 기준.

Swift·Kotlin 비교 학습 적용 시작일: 2026-07-15 (수), KST 기준.

읽기·선택 퀴즈 방식 적용 시작일: 2026-07-22 (수), KST 기준.

- Daily Note는 시간표나 회고장이 아니라 오늘 읽을 자료와 완료 체크만 보여 주는 개인 학습 큐다.
- 회사 업무, 수강생 과제, 강의 준비, 날씨, 이동 시간, 출퇴근 중 할 일은 Daily Note에 넣지 않는다.
- 시간대별 블록을 만들지 않는다. 일정이 바뀌어도 읽을 수 있도록 `핵심 학습`, `일일 루틴`, `선택 확장`으로만 나눈다.
- Daily Note는 새 읽기·선택 퀴즈 또는 전날에서 이월한 Mobile 파일 1개, 새 자료 또는 이월한 Go·React 풀스택 파일 1개, 새 자료 또는 이월 자료인 IT 동향 1개, 새 전날 주식시장 브리핑 1개, 새 페이지 또는 이월 페이지인 노션 투자 학습 링크 1개를 기본값으로 한다. 오늘의 상식은 3개 큐별 미완료 항목을 모두 이월하고, 미완료가 없는 큐만 새 자료 1개를 둔다. 선택 확장은 사용자가 활성화한 주제가 있을 때만 추가한다.
- 영어회화와 정보처리기사는 필요할 때만 선택 루틴으로 넣고, 특정 장소나 시간에 묶지 않는다.
- Swift·Kotlin 학습은 15~25분을 기준으로 `설명 읽기 → 개념 보기 선택 → 코드 결과 또는 올바른 코드 선택 → Swift·Kotlin 차이 선택 → 정답·해설 확인` 순서로 진행한다.
- 워크북은 코드를 보여주되 사용자가 직접 입력·컴파일·실행·재작성하도록 요구하지 않는다.
- 단일 선택과 복수 선택을 문제마다 명시하고, 옳은 것과 틀린 것 중 무엇을 고르는지도 각 문제에 명시하며 모든 보기는 체크박스로 제공한다.
- 정답과 해설은 답을 고른 뒤 확인할 수 있도록 기본으로 닫힌 `> [!answer]-` 콜아웃으로 접는다.
- 새 개념 Day 5개마다 누적 복습 퀴즈를 배치하며, 이 역시 읽고 선택하는 형식으로 진행한다.
- 피로하거나 일정이 흔들린 날은 새 목표를 늘리지 말고 핵심 읽기 1개만 완료한다.
- 누적 복습 이후에는 포트폴리오 정리나 블로그 초안화를 별도 요청으로 진행한다.

---

### Daily 피드백·기록 운영

- Daily에는 `전날 피드백 확인`, `학습 메모`, `실습/프로젝트 메모`, `오늘 하루 정리`, `오늘 배운 개념 연결`, `오늘 하루 피드백`, `저녁 회고`를 만들지 않는다.
- "오늘 내용을 피드백해줘" 요청이 와도 Daily에 피드백 섹션을 작성하거나 커밋하지 않는다. 필요하면 채팅에서만 간단히 답한다.
- Daily와 그날 생성한 학습·읽기 파일의 작업 단위 커밋은 아침 1단계에서 계속 유지한다.

---

### 일요일 아침: "오늘 해야될 것을 알려줘" (일요일 한정 추가)

일요일 아침에는 평소 계획 응답에 **진로 대화 세션**을 추가한다.

```
## 🌅 오늘의 계획 (YYYY-MM-DD, 일요일)

[평소 아침 계획 형식 동일]

---

## 🧭 이번 주 진로 탐색

> 현재 탐색 중인 방향: [Profile.md 기반으로 현재 가장 가능성 있는 방향 1-2개 제시]

**이번 주 질문**
- [진로와 관련된 구체적 질문 1개 — 예: "On-device AI 방향으로 갈 때 Core ML 학습을 먼저 할까요, 아니면 포트폴리오 앱부터 잡을까요?"]

> 답변해 주시면 다음 주 방향성을 같이 구체화합니다.
```

진로 대화 진행 원칙:
- 한 번에 결론을 내리지 않는다. 매주 조금씩 좁혀간다.
- 사용자 답변을 받으면 `Profile.md`의 "관심 방향" 섹션 갱신을 제안한다.
- 헬스케어 도메인 + AI + 모바일이라는 희소한 조합을 강점으로 활용하는 방향을 우선 탐색한다.

---

### 일요일 저녁: 주간 회고

1. 해당 주 Daily Note 7개를 읽고 흐름을 파악한다.
2. 회고는 **대화** 형식으로 진행한다 — 사용자 답변을 기다린 후 다음 단계로 넘어간다.

```
① 이번 주 요약 (완료 / 미완료 / 인사이트)
② 질문 2-3개 제시 → 사용자 답변 대기
③ 답변 반영 → 다음 주 방향성 초안 제안
④ 사용자 확인/수정 후 확정
⑤ Weekly Review 노트 작성 안내
```

---

## 단기 목표 관리

목표 설정 요청 시 아래 형식으로 정리한다.

```
목표: [무엇을 만들거나 정리할 것인가]
기간: [3일 / 5일 / 1주 / 2주] → 마감: YYYY-MM-DD
체크포인트:
  - D1: [할 것]
  - D3: [할 것]
  - 완료 기준: [Definition of Done]
```

목표가 활성화된 경우, 아침 계획에 진행 상황을 반드시 포함한다.
평일 단기 목표는 시간대가 아니라 `핵심 학습`, `일일 루틴`, `선택 확장`, `마무리`에 들어갈 만큼 작은 단위로 쪼갠다.

---

## 진로 상담 기준

핵심 포지셔닝은 `iOS를 기반으로 Android 네이티브까지 확장한 모바일 엔지니어`로 확정되었다. 세부 도메인과 지원 포지션은 매주 일요일 아침 대화를 통해 계속 좁혀간다.

취업 방향 제안 시 반영 요소:
- 의료기기 펌웨어 3년 1개월 + 헬스케어 앱(iOS/Android) + AI 헬스케어 부트캠프
- Swift·iOS를 주력으로 유지하면서 Kotlin·Android 네이티브를 채용 지원 가능한 수준까지 확장
- On-device AI(Core ML 등) — 모바일 내부에 AI를 내장하는 방향 탐색 중
- 멘토링·강의 병행 가능한 포지션 선호
- 헬스케어 도메인 전문성이 핵심 차별점

포지션 탐색 풀:
- iOS 기반 Android 네이티브 확장 모바일 엔지니어
- iOS + On-device AI (Core ML, Apple Intelligence)
- AI 헬스케어 모바일 앱
- BLE·IoT·실시간 건강 데이터 모바일 서비스
- 헬스케어 AI 멘토링/교육 콘텐츠

기술 스택이나 방향이 바뀌면 `Profile.md`의 "관심 방향" 갱신을 제안한다.

---

## 파일 구조

```
zerohyeon-labs/
├── Daily/              # 월별 폴더(YYYY-MM/) 안에 YYYY-MM-DD.md (일별 학습 노트)
├── Weekly/             # 주간 회고 노트
├── Schedule/           # 일정 및 목표 관리
│   ├── 단기 목표/      # 3일~2주 단기 목표
│   ├── 장기 로드맵/    # 커리어 방향 로드맵
│   └── 근무 후 학습 루틴.md
├── Projects/           # 개발 프로젝트 (구: 개발 Project)
├── Learning/           # 기술 학습
│   ├── iOS/
│   ├── AI Study/
│   ├── Backend/
│   ├── IT Trends/
│   ├── Stock Basics/
│   └── CS (Computer Science)/
├── Conversations/      # 주제별 학습 대화 기록
│   ├── iOS Mentoring/
│   ├── Swift Study/
│   ├── AI Mentoring/
│   ├── Backend Mentoring/
│   ├── Python Study/
│   └── Mobile Mentoring/
├── Interview/          # 면접 대비 (Q&A, 경험 정리)
├── Blog/               # 블로그 초안 및 발행 관리
│   ├── 초안/
│   └── 발행됨/
├── Career/             # 진로 탐색
│   ├── 방향 탐색/
│   └── 포트폴리오/
├── Assets/             # 이미지, 다이어그램
├── Templates/          # 노트 템플릿 + 자동화 스크립트
│   └── auto_review.sh  # cron 매일 23:00
├── Scripts/            # 유틸리티 스크립트
├── Profile.md          # 사용자 프로필 및 목표
└── AGENTS.md           # 이 파일
```

> **Note**: `Learning/`, `Projects/` 폴더로의 실제 파일 이동은 Obsidian에서 직접 수행해야 내부 링크가 자동으로 업데이트됩니다.

---

## 행동 원칙

- 트리거 감지 시 Daily Note를 **먼저 읽고** 응답한다.
- 아침 계획 트리거에서는 Daily Note가 없으면 새로 생성한다.
- 피드백/회고 트리거에서는 Daily Note가 없으면 생성 안내 후 멈춘다. 내용을 임의로 꾸며내지 않는다.
- 피드백은 항상 구체적·기술적으로 한다. 추상적 격려는 하지 않는다.
- 주간 회고는 보고서가 아닌 대화다. 사용자 답변 전에 다음 단계로 넘어가지 않는다.
- Daily Note 생성에는 회사 업무, 날씨, 이동 시간, 시간대별 블록을 넣지 않는다.
- 신기술 동향은 Daily Note에 짧게 요약하고, 별도 읽기 페이지에 출처와 연결 지점을 남긴다.
- Obsidian의 주식 기초 파일은 Daily Note에 자동 생성하지 않는다. 3단계에서 오늘 사용할 것으로 확정한 노션 투자 학습 페이지가 있으면 신규 페이지인지 전날 이월 페이지인지와 관계없이 실제 링크만 연결한다. 투자 학습은 순수 지식 읽기로만 구성하고 개인 자산 적용·문제·피드백을 제외하며, 종목 추천·매수/매도 판단·수익률 예측을 넣지 않는다.
- 시간대는 KST(UTC+9) 기준으로 판단한다.
- `Profile.md`와 `AGENTS.md`는 목표, 기술 스택, 학습 루틴이 바뀌면 갱신을 제안한다.
